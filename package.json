{
  "name": "bekhruz-tracker",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "install-server": "cd server && npm install",
    "start": "node server/index.js",
    "dev": "cd server && node --watch index.js"
  }
}
